<Page
    x:Class="Employees.AddEmployee"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Employees"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    
        <Page.BottomAppBar>
            <CommandBar >
                <AppBarButton Icon="Add" Label="Add" Click="ButtonBase_OnClick"></AppBarButton>
                <AppBarButton Icon="Cancel" Label="Cancel" Click="ButtonBase_OnClick1"></AppBarButton>
            </CommandBar>
        </Page.BottomAppBar>

    <Grid>

        <StackPanel>
           <TextBox x:Name="EmployeeNameTb" PlaceholderText="Write your Name" Padding="20"></TextBox>
            <TextBox x:Name="EmployeeDeptTb" PlaceholderText="Write your Department" Padding="20.0,20"></TextBox>
        </StackPanel>

    </Grid>
</Page>
